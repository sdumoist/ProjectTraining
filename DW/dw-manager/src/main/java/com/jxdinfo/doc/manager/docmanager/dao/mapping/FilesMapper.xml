<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxdinfo.doc.manager.docmanager.dao.FilesMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.jxdinfo.doc.manager.docmanager.model.FsFile">
        <id column="file_id" property="fileId" />
        <result column="file_name" property="fileName" />
        <result column="file_icon" property="fileIcon" />
        <result column="file_type" property="fileType" />
        <result column="file_path" property="filePath" />
        <result column="file_size" property="fileSize" />
        <result column="create_time" property="createTime" />
        <result column="file_pdf_path" property="filePdfPath" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        file_id AS fileId, file_name AS fileName, file_icon AS fileIcon, file_type AS fileType, file_path AS filePath, file_size AS fileSize, create_time AS createTime, file_pdf_path AS filePdfPath
    </sql>

    <select id="getParentIdByFoldId" resultType="String">
        select distinct ff.parent_folder_id
        from fs_folder ff
        WHERE
        1 = 2
        <if test="idList != null and idList.size() >0">
            OR ff.folder_id IN
            <foreach item="idItem" collection="idList" open="(" separator="," close=")">
                #{idItem}
            </foreach>
        </if>
    </select>
</mapper>
